# t81-docs

Central documentation hub for the `t81dev` ecosystem.

## Start Here

- `docs/ecosystem-overview.md`: repo map grouped by function and dependency flow.
- `docs/repository-inventory.md`: complete inventory of all public repositories under `t81dev`.
- `docs/repositories.md`: index of generated per-repository documentation pages.
- `docs/runtime-contract.md`: runtime compatibility boundary for `t81-vm` consumers.
- `docs/runtime-sync-status.md`: weekly runtime compatibility/check health snapshot mirrored from `t81-roadmap`.
- `docs/repos/`: one generated page per repository.
- `docs/repos.json`: machine-readable repository metadata for automation.
- `docs/maintenance.md`: sync and maintenance workflow.

## Scope

This repository is intended to become the single entry point for:

- architecture overviews
- installation and setup paths
- API and interface references
- balanced ternary explainers
- migration guides from binary-native systems

## Current Status

Initial ecosystem index is in place and includes all currently visible repositories at:

- `https://github.com/t81dev`

Snapshot date: `2026-02-08`.

## Maintenance

Sync all generated ecosystem docs from the GitHub API:

```bash
./scripts/sync-ecosystem-docs.sh t81dev
```

Generated by:

- `scripts/generate-repo-metadata.sh`
- `scripts/generate-repo-pages.sh`
- `scripts/generate-repo-inventory.sh`

Validation and integrity checks:

- `scripts/validate-repo-metadata.sh`
- `scripts/check-doc-links.sh`
- `scripts/check-runtime-contract-sync.py`

Automation:

- `.github/workflows/docs-validate.yml`
- `.github/workflows/docs-sync.yml`
- `.github/workflows/runtime-sync-status.yml`
